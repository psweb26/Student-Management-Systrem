# Student-Management-Systrem
Student Management System with Java Spring Boot backend, MySQL database, and static frontend (HTML/CSS/JS). Includes schema/data SQL files, Maven build, and ready-to-run local setup. Clean repo structure with .gitignore and example config.
